[project]
name = "supportopsenv"
version = "0.1.0"
description = "Risk-aware RL environment for AI customer support"
authors = [
    { name="Pooja", email="you@example.com" }
]

dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "app.server:app"

[tool.setuptools]
packages = ["app"]